<?xml version="1.0" encoding="UTF-8"?>
<!--
  iOS / watchOS WidgetKit extension Info.plist 템플릿.

  왜 명시 Info.plist 가 필요한가:
  - xcodegen 의 `INFOPLIST_KEY_NSExtensionPointIdentifier` 가 NSExtension dict
    로 자동 변환되지 않음 (iOS, watchOS 양쪽 다). ASC validation 이
    `90360 Missing Info.plist value` 로 reject.
  - GENERATE_INFOPLIST_FILE: NO + INFOPLIST_FILE 으로 이 파일을 직접 사용.

  앱 repo 에 복사 후 수정:
  1. CFBundleDisplayName 만 앱별로 변경 (한국어 / 영문 어느 쪽이든).
  2. 그 외 키들은 $(...) 빌드 변수 — 그대로 두면 됨.

  watchOS extension 에는 LSRequiresIPhoneOS 키가 없어야 한다 (있으면 watch
  install 거부됨). iOS / watch 둘 다 같은 파일을 쓰려면 LSRequiresIPhoneOS
  를 빼는 게 안전. (개별로 사용하려면 iOS 쪽에 추가.)
-->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>
    <string>$(DEVELOPMENT_LANGUAGE)</string>
    <key>CFBundleDisplayName</key>
    <string><WIDGET_DISPLAY_NAME></string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundlePackageType</key>
    <string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>
    <key>NSExtension</key>
    <dict>
        <key>NSExtensionPointIdentifier</key>
        <string>com.apple.widgetkit-extension</string>
    </dict>
</dict>
</plist>
